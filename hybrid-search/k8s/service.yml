apiVersion: v1
kind: Service
metadata:
  name: search-api
  labels:
    app: search-api
spec:
  selector:
    app: search-api
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP