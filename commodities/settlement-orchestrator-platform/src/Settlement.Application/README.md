# Settlement.Application

Application layer for commands, queries, workflow orchestration use cases, and ports.

This layer coordinates domain behavior without depending on Oracle, HTTP, or background worker infrastructure.

