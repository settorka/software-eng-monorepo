

services:
  n8n:
    image: n8nio/n8n:latest
    ports:
      - "5678:5678"
    environment:
      - N8N_HOST=0.0.0.0
      - N8N_PORT=5678
      - NODE_ENV=production
      - TZ=Europe/London
    volumes:
      - n8n_data:/home/node/.n8n
    restart: always

volumes:
  n8n_data:
